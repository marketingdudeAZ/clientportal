flask==3.0.3
gunicorn==22.0.0
waitress==3.0.2
requests==2.32.3
python-dotenv==1.0.1
PyJWT==2.9.0
# Pinned, not floated: anthropic 1.0.0 (2026-08-20) is a major release that
# moves the SDK to httpx 2. A redeploy on 2026-08-24 picked it up from the
# old `>=0.40.0` and every ticket recap went silently empty for three days
# (ticket_recap.generate_recap swallows the exception into an empty note).
# 0.125.0 is the last 0.x and is verified against the live recap path.
anthropic==0.125.0
Pillow>=10.0.0
gspread==6.1.2
google-auth==2.29.0
google-api-python-client==2.128.0
google-cloud-bigquery>=3.25.0
google-cloud-storage>=2.10.0
reportlab>=4.0.0
cryptography>=42.0.0  # RS256 verify for Clerk session JWTs (clerk_auth.py)
