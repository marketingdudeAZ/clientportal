[build]
watchPatterns = ["webhook-server/**"]

[deploy]
startCommand = "cd webhook-server && gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
